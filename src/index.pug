doctype html
html(lang="ru")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title To-Do list
  body
    header.header
      h1.header__title To-Do list
      select.header__select
        option.header__option(value="default") Default theme
        option.header__option(value="dark") Dark theme
    main.main
      .form-section
        form.form
          input.form__task-name(type="text" placeholder="Введите название задачи" required)
          textarea.form__task-desc(placeholder="Введите описание задачи" required)
          button.form__btn(type="submit") Добавить задачу
      .tasks-section
        .tasks-section__container



